{
  "name": "@ride/api",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "main": "./server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "deploy": "now",
    "test": "cross-env NODE_ENV=test nyc --reporter=text --reporter=text-summary mocha --exit --require test/setup.js \"test/**/*.test.js\"",
    "watch": "cross-env NODE_ENV=test mocha --watch --require test/setup.js \"test/**/*.test.js\""
  },
  "dependencies": {
    "express": "^4.16.3",
    "request": "^2.88.0",
    "request-promise-native": "^1.0.5"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "cross-env": "^5.2.0",
    "mocha": "^5.2.0",
    "nodemon": "^1.18.4",
    "nyc": "^13.0.1"
  }
}
