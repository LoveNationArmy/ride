{
  "name": "@ride/web",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "dependencies": {
    "@ride/stigma": "^0.1.0",
    "enzyme": "^3.7.0",
    "enzyme-adapter-react-16": "^1.6.0",
    "react": "^16.4.2",
    "react-dom": "^16.4.2",
    "react-scripts": "1.1.4"
  },
  "devDependencies": {
    "dotenv": "^6.1.0",
    "fetch-mock": "^7.2.2",
    "mock-local-storage": "^1.1.7"
  },
  "scripts": {
    "deploy": "now",
    "dev": "yarn start",
    "start": "HTTPS=true react-scripts start",
    "build": "react-scripts build",
    "eject": "react-scripts eject",
    "test": "CI=true yarn test:run",
    "test:watch": "yarn test:run --coverage --watch",
    "test:coverage": "yarn test:run --coverage",
    "test:run": "react-scripts test --env=jsdom --setupTestFrameworkScriptFile=./test/setup.js"
  }
}
